# Source files

The application source and deployment project files for this task are being added to this public repository from the completed local solution.

Do not commit generated binaries, MSI/MSIX packages, or Visual Studio build output. See the repository root `.gitignore`.
